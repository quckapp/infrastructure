# =============================================================================
# QA Backend Configuration
# =============================================================================
# Copy to backend.hcl and update with your values

bucket         = "quckapp-terraform-state-ACCOUNT_ID"
key            = "qa/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "quckapp-terraform-locks"
encrypt        = true
